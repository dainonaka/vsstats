%html
  %body
    %ul.entries
      -for entry in entries:
        %li
          {{ entry.date | safe }}
          【{{ entry.name | safe }}】さんが
          【{{ entry.opponent | safe }}】さんに
          -if entry.win == 2
            【勝利】
          -else
            【敗北】
          しました。
          コメント：{{ entry.comment | safe }}
    %p
      ↓{{ current_user.name | safe }}さん、戦績を入力しましょう！
    %form(action="/post" method="POST")
      %input(type="text" name="opponent" placeholder="対戦相手（10文字まで）")
      %br
      %input(type="radio" name="win" value="2")
      勝利！
      %input(type="radio" name="win" value="1")
      敗北…
      %br
      %input(type="text" name="comment" placeholder="コメント（20文字まで）")
      %input(type="submit")

    %a(href="/logout")
      ログアウト